name: CI and Publish

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    secrets: inherit

  docker-publish:
    needs: ci
    uses: ./.github/workflows/docker-publish.yml
    secrets: inherit
